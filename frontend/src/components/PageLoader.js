import React from "react";
import "./PageLoader.css"; // Style as desired

const PageLoader = () => (
  <div className="page-loader">
    <div className="spinner" />
    <p>Loading page...</p>
  </div>
);

export default PageLoader;
